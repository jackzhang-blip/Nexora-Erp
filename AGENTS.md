# Nexora ERP 开发规范

本文档适用于整个仓库。更深层目录中的 `AGENTS.md` 可以补充或覆盖本文件中与该目录相关的规则。

## 项目概况

- 这是一个 Electron + Vue 3 + TypeScript 桌面应用，后端基础服务使用 FastAPI。
- `src/main` 放 Electron 主进程代码，`src/preload` 放受限的预加载桥接，`src/renderer` 放 Vue 渲染层，`src/shared` 放前后端共享类型或协议。
- `backend/app` 放 Python 后端，`backend/tests` 放后端测试，`tests` 放仓库级测试。
- README 中标记为“计划”“候选”或“设计目标”的能力，不得在代码、提交说明或文档中描述为已经实现。

## 开发流程

1. 一个具体需求对应一个独立分支，不在主分支直接开发。分支名使用 `codex/<简短需求名>`，并保持只服务于一个需求。
2. 同一工作区同一时间只允许有一个未完成的需求分支。开始工作前先检查当前分支和工作区状态：
   - 当前已经是未完成的 `codex/*` 分支时，继续在该分支完成原需求，不切换分支，也不为新消息创建分支。
   - 当前在主分支且没有未完成的需求分支时，才能为新需求创建一个 `codex/*` 分支。
   - 如果发现已有未完成需求分支但当前不在该分支，先回到该分支继续原需求；不得通过新建分支绕开它。
3. 已有需求分支尚未完成时，后续需求只能排队，不能开新分支、切换到其他需求分支或把两个需求混在当前分支中。当前需求的“完成”必须同时满足：实现完成、对应单元测试编写并通过、必要的类型检查和构建通过、相关文档已同步。
4. 修改前先阅读相关模块、类型定义和现有测试，遵循已有实现方式。
5. 保持改动范围与需求一致，不顺手重构无关代码，不覆盖用户已有的未提交修改。
6. 实现需求后必须编写并完成对应的单元测试；测试应覆盖新增行为、主要失败路径和回归风险。
7. 完成修改后根据影响范围运行验证：至少运行 `npm run typecheck`；涉及构建时运行 `npm run build`；涉及后端时运行对应的 `pytest` 命令。
8. 如果无法运行某项验证，说明具体原因，不要把未执行的检查写成已通过；测试未通过时不得合并。
9. 当前需求完成后，先提交并推送需求分支，提交合并请求并合并到主分支；合并前确认变更范围、测试结果和文档状态。合并完成后删除已合并的需求分支，确认工作区回到主分支并保持干净，之后才能为下一个需求创建新分支。
10. 新增行为时优先补充能验证真实风险的测试，避免只为提高覆盖率编写重复实现的测试。

## 代码规范

- 默认使用 TypeScript 严格类型，避免 `any`；共享数据优先定义明确的接口或类型。
- Vue 组件使用现有项目的 Vue 3 写法和样式组织方式，保持页面状态、业务逻辑和进程通信边界清晰。
- 主进程与渲染进程之间只通过预加载层暴露必要能力，不直接把 Node.js 或 Electron 高权限对象暴露给渲染进程。
- 用户输入、文件路径、网络响应和 IPC 参数都要在边界处校验；错误需要提供可诊断的信息，同时避免泄露敏感数据。
- Python 代码遵循现有 FastAPI 结构，路由、业务逻辑和数据访问不要无理由混在同一个函数中。
- 开发注释尽量使用中文。注释说明原因、约束或不明显的决策，不重复描述代码本身。
- 命名使用清晰、稳定的英文标识符；面向用户的界面文案遵循项目现有语言和产品术语。

## Electron 安全

- 默认保持 `contextIsolation`、受限的 preload API 和最小权限原则。
- 不使用 `nodeIntegration` 绕过进程边界；新增 IPC 通道时同时检查调用方、参数和返回值。
- 外部链接、子进程、文件系统和网络访问必须有明确的调用边界，并考虑失败、取消和资源释放。

## 后端与数据

- 财务、库存、人力和 CRM 数据变更应保留可审计的信息，不能未经说明地使用“最后写入覆盖”作为通用冲突策略。
- 数据库结构、同步协议和冲突处理在尚未正式确定前，应明确标注为候选方案或设计草案。
- API 响应格式和错误语义一旦被前端使用，修改时要同步更新调用方、类型和测试。

## 验证命令

在仓库根目录执行：

```text
npm run dev
npm run typecheck
npm run build
npm run preview
```

后端相关变更先阅读 `backend/README.md`，并按其中的环境说明执行测试。不要为了通过检查删除或跳过已有测试。

## 文档与提交

- 文档中的实现状态必须与代码一致，区分“已实现”“基础能力”“候选技术”和“计划”。
- 修改公共接口、运行方式、目录职责或架构决策时，同步更新相关 README 或设计文档。
- 提交信息应简洁说明实际变化，避免把格式化、重命名或计划中的功能写成已完成。
- 不提交密钥、令牌、本地数据库、构建产物或包含个人信息的调试文件；遵循 `.gitignore`。

## 交付说明

完成任务时说明：改了什么、为什么这样改、运行了哪些验证，以及仍然存在的限制或风险。若需求只涉及文档或低风险修改，也要明确说明未执行的重型验证。
